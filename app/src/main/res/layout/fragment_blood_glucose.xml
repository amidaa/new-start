<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragment.BloodGlucoseFragment">

   <de.hdodenhof.circleimageview.CircleImageView
       android:id="@+id/blood_img"
       android:layout_width="50dp"
       android:layout_height="50dp"
       android:src="@drawable/carrot"
       android:layout_centerInParent="true"
       android:layout_marginRight="50dp"
       android:layout_marginLeft="50dp"/>
    <!--空腹血糖含量-->
    <TextView
        android:id="@+id/FBG"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@+id/blood_img"
        android:layout_alignTop="@+id/blood_img"
        android:text="@string/FBG"/>
    <!--饱餐后血糖含量-->
    <TextView
        android:id="@+id/after_meal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/blood_img"
        android:layout_alignTop="@id/blood_img"
        android:text="@string/after_meal"/>

</RelativeLayout>